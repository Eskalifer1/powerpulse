"use client";

import { styled } from "styled-components";

export const UserInfoDropDownWrap = styled.div`
  position: absolute;
  top: -3rem;
  left: -1.6rem;
  padding: 0.5rem;
  background: var(--background-color);
  border-radius: 0.5rem;
`;

export const UserInfoDropDownButton = styled.button`
  width: 100%;
  background: none;
  color: var(--text-color);
  font-family: inherit;
`;
