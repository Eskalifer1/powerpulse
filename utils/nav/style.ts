"use client";
import { BiBody, BiDumbbell } from "react-icons/bi";
import { styled } from "styled-components";

export const Dumbell = styled(BiDumbbell)`
  width: var(--nav-links-size);
  height: var(--nav-links-size);
`;

export const Body = styled(BiBody)`
  width: var(--nav-links-size);
  height: var(--nav-links-size);
`;
