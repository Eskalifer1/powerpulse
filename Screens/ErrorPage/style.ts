"use client";

import styled from "styled-components";

export const ErrorPageImageWrap = styled.div`
  position: relative;
  aspect-ratio: 4 / 3;
  width: var(--help-page-image);
`;

export const ErrorTitle = styled.h1`
  margin-block-start: 2rem;
`;
